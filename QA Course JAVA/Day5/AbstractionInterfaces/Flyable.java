package Day5.AbstractionInterfaces;

interface Flyable { //inherently public, all methods in interfaces are abstract; dont need to use keyword
    void spreadWings();
    void takeOff(); //interfaces contain abstract methods
}